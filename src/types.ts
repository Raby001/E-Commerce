export interface category {
  image: string
  name: string
  number: number
  background: string
}

export interface promotion {
  text: string
  background: string
  image: string
  buttonColor: string
  buttonText: string
}
