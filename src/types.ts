export interface category {
  image: string
  name: string
  number: number
  background: string
}

export interface promotion {
  title: string
  background: string
  image: string
  buttonColor: string
  buttonText: string
}

export interface navbarCat {
  bold: boolean
  title: string
}
