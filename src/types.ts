export interface category {
    name: string;
    productCount: number;
    color: string;
    image: string;
    group?: string;
}

export interface promotion {
  title: string
  buttonColor: string
  color: string
  image: string
  url?: string
}